package org.jtalks.tests.jcommune.pages;

import org.openqa.selenium.WebDriver;

/**
 * @autor masyan
 */
public interface Page {
	//Initialise elements. Initialization start, when test use element
	void init(WebDriver driver);
}
